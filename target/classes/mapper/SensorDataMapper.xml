<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.agri.platform.mapper.SensorDataMapper">
    <!-- 查询某块土地的最新数据（按采集时间倒序取第一条） -->
    <select id="selectLatestByLandId" resultType="com.agri.platform.entity.SensorData">
        SELECT * FROM sensor_data WHERE land_id = #{landId} ORDER BY recorded_at DESC LIMIT 1
    </select>

    <!-- 按土地ID和时间范围查询历史数据 -->
    <select id="selectByLandIdAndTimeRange" resultType="com.agri.platform.entity.SensorData">
        SELECT * FROM sensor_data
        WHERE land_id = #{landId}
        AND recorded_at BETWEEN #{startTime} AND #{endTime}
        ORDER BY recorded_at ASC
    </select>
</mapper>